type Employee = {
  id?: string;
  email: string;
  birthDate: string;
  startDate?: string;
};

type Message = {
  id?: string;
  title: string;
  text: string;
};

type User = {
  isAdmin: boolean;
};
