type Employee = {
  id: string;
  email: string;
  birthdate: number;
  startDate: number;
};

type Message = {
  id?: string;
  title: string;
  text: string;
};

type User = {
  isAdmin: boolean;
};
